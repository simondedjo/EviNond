<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.app_cpslab.android.evinond">

    <uses-feature
        android:glEsVersion="0x00020000"
        android:required="true" />


    // Add permissions to Manifest file for ArcGIS

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />



    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        //Declare ReportFlood activity
        <activity android:name=".ReportFlood"
            android:label="Report a Flood"
            android:theme="@style/AppTheme"/>

        //Declare AddLocation activity
        <activity android:name=".AddLocation"
            android:label="Refine flood location"
            android:theme="@style/AppTheme"/>

        //Declare AddFloodInfo activity
        <activity android:name=".AddFloodInfo"
            android:label="Add Flood Details"
            android:theme="@style/AppTheme"/>

        //Declare FloodMap activity
        <activity android:name=".FloodMap"
            android:label="Flood Alert"
            android:theme="@style/AppTheme"/>


        //Declare FloodMap activity
        <activity android:name=".RoutingSample"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="Geo Routing"
            android:theme="@style/AppTheme"/>

        <!-- ATTENTION: This was auto-generated to add Google Play services to your project for
     App Indexing.  See https://g.co/AppIndexing/AndroidStudio for more information. -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

    </application>

</manifest>